{% extends 'base.html.twig' %}

{% block title %} Quotable
{% endblock %}

{% block body %}
	
    <div class="card text-center mt-5 col-6 mx-auto">
           <div class="card-body">
               <p class="card-text">{{data.content}}</p>
               <h3 class="card-footer text-dark mx-2">{{ data.author }}</h3>
           </div>
    </div>
{% endblock %}
